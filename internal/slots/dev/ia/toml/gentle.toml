id = "gentle"
category = "dev"
subcategory = "ia"
dependencies = []

[install]
cmd = "curl -fsSL https://github.com/gentle-ai/gentle/releases/latest/download/gentle-linux-amd64 -o /tmp/gentle && mkdir -p \"$HOME/.local/bin\" && mv /tmp/gentle \"$HOME/.local/bin/gentle\" && chmod +x \"$HOME/.local/bin/gentle\""
