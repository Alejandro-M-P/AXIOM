id = "sqlite"
name = "SQLite"
description = "Base de datos embebida, sin servidor"
category = "data"
subcategory = "databases"
dependencies = ["sqlite"]

[install]
cmd = "sudo pacman -S --noconfirm sqlite"
