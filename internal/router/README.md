# Router — Navigation

This package handles **command routing** and coordinates the flow between user input and business logic.

## What goes here

- CLI command handlers
- Navigation between screens
- Initial context setup

## Rules

- Entry point for user commands
- Uses `bunker/`, `build/`, `slots/` for business logic
- Only reads config for initialization
