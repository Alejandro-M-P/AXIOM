# Config — Configuration

This package handles **configuration loading** from TOML files.

## What goes here

- `EnvConfig` struct — environment configuration
- Path helpers
- Config file location and loading

## Rules

- Reads from config.toml
- Provides data to other packages
- **No business logic**
